import React from 'react';
import Tree from '../components/tree';

import {
  Container,
} from './styles';

const Main = (params) => {
  return (
    <Container>
      <Tree />
    </Container>
  )
}

export default Main;
